# agentic-rag-for-dummies

following [Agentic RAG for Dummies](https://github.com/GiovanniPasq/agentic-rag-for-dummies)

## notable differences from the og

- `granite4:tiny-h` instead of `qwen3:4b-instruct-2507-q4_K_M` as the model
- `marimo` instead of `jupyter`
- `uv` to take care of env and deps instead of bare `pip`
